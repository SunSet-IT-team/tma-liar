import { GameMessageTypes } from "./game.types";
import { LobbyMessageTypes } from "./lobby.types";

/**
 * Объект всех типов сообщений
 */
export const MessageTypes = {
    Lobby: LobbyMessageTypes,
    Game: GameMessageTypes,    
}